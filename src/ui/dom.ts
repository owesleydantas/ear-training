export const playButton = document.getElementById("play-chord") as  HTMLButtonElement;

export const repeatButton = document.getElementById("repeat-chord") as HTMLButtonElement;

export const feedback = document.getElementById("feedback") as HTMLDivElement;

export const hitsDisplay = document.getElementById("hits") as HTMLSpanElement;

export const missesDisplay = document.getElementById("misses") as HTMLSpanElement;

export const varyRootCheckbox = document.getElementById("vary-root") as HTMLInputElement;

export const answerButtons = document.querySelectorAll<HTMLButtonElement>("#answer button");